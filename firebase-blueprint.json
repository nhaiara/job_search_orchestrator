{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Master profile data for the user, including skills, experience, and AI preferences.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string" },
        "masterProfile": { "type": "string", "description": "The full Master Profile text (Markdown) - Cached from Drive." },
        "masterProfileFileId": { "type": "string", "description": "The Google Drive File ID for the Master Profile document." },
        "customRules": { "type": "string", "description": "Custom rules for Gemini to classify or disqualify jobs." },
        "driveFolderId": { "type": "string", "description": "The Google Drive folder ID to monitor for .md files." },
        "outputFolderId": { "type": "string", "description": "The Google Drive folder ID where generated applications will be saved." },
        "processedFolderId": { "type": "string", "description": "The Google Drive folder ID where processed .md files will be moved." },
        "cvDiamondFileId": { "type": "string", "description": "The Google Drive File ID for CV Diamond." },
        "cvGoldFileId": { "type": "string", "description": "The Google Drive File ID for CV Gold." },
        "cvSilverFileId": { "type": "string", "description": "The Google Drive File ID for CV Silver." },
        "clDiamondFileId": { "type": "string", "description": "The Google Drive File ID for Cover Letter Diamond." },
        "clGoldFileId": { "type": "string", "description": "The Google Drive File ID for Cover Letter Gold." },
        "generationRules": {
          "type": "object",
          "properties": {
            "tags": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "name": { "type": "string" },
                  "instruction": { "type": "string" }
                }
              }
            },
            "globalRules": { "type": "string" },
            "silverPrompt": { "type": "string" },
            "goldPrompt": { "type": "string" },
            "diamondPrompt": { "type": "string" }
          }
        },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "masterProfile"]
    },
    "JobApplication": {
      "title": "Job Application",
      "description": "Details of a specific job application and its status.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "uid": { "type": "string" },
        "company": { "type": "string" },
        "role": { "type": "string" },
        "location": { "type": "string" },
        "status": { 
          "type": "string", 
          "enum": ["📥 Nova", "⏳ Input IA", "⚙️ Gerar Docs", "✅ Aplicar", "📩 Triagem", "🕰️ Feedback", "🗣️ Entrevistas", "🏆 Proposta", "🤝 Sucesso", "❌ Rejeitada", "🗑️ Descarte"] 
        },
        "matchScore": { "type": "string", "enum": ["Diamond", "Gold", "Silver", "Discard"] },
        "totalScore": { "type": "number" },
        "matchReasoning": { "type": "string" },
        "goldenPillar": { "type": "string" },
        "diamondQuestions": { "type": "array", "items": { "type": "string" } },
        "discardReason": { "type": "string" },
        "jobDescription": { "type": "string" },
        "link": { "type": "string" },
        "driveFileId": { "type": "string", "description": "ID of the source .md file in Google Drive" },
        "driveFolderLink": { "type": "string", "description": "Link to the specific application folder in Drive" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "company", "role", "status"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "User specific profile and settings."
    },
    "/users/{userId}/applications/{applicationId}": {
      "schema": "JobApplication",
      "description": "Applications tracked by the user."
    }
  }
}
